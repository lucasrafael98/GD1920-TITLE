/// @desc Add a string to a dialog
/// @arg dialog The dialog the add the string to
/// @arg string The string to add

var __map = global.dialog_map_strings;
ds_map_replace(__map, argument0, argument1);