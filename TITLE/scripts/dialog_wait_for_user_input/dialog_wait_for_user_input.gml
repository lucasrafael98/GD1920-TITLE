/// @desc Gets whether the dialog system should wait for user input before progressing in the current dialog string
///		  This helps with strings that are larger than the available screen space, so the dialog can pause and allow the user to progress at their own pace

// Disabled in the demo because this is very game-specific
return false;